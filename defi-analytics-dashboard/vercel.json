{
  "version": 2,
  "buildCommand": "npm run fix-swc && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}